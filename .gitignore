# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets (Vite: .env.local already matched by *.local; ignore plain .env too)
.env
.env.*
!.env.example

# Google OAuth / credential downloads — never commit
.secrets/
**/client_secret*.json
*credentials*.json
!.env.example

# Internal docs — keep out of remote (README stays tracked)
*.md
!README.md
!LOADING_ANIMATIONS.md

# Vercel CLI (project link; keep out of git)
.vercel

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Env / secrets (Vite: .env.local already matched by *.local; ignore plain .env too)
.env
.env.*
!.env.example

# Google OAuth / credential downloads — never commit
.secrets/
**/client_secret*.json
*credentials*.json
!.env.example

# Internal docs — local only, never push (README at repo root stays tracked)
docs/
*.md
!README.md

# Legacy data migration (local only — contains space/member IDs)
scripts/migrate_legacy*.sql

# Capacitor / Xcode build artifacts (ios/ project itself is generated on Mac — see docs/IOS_WIDGET.md)
android/
.capacitor/
**/xcuserdata/
**/DerivedData/
*.xcuserstate


# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
